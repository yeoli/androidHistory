<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yeol.jolup1">
<uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"
            android:label="홈으로"
            android:theme="@style/hopeTheme">
        </activity>
        <activity android:name=".FiveTabActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".IntroActivity" android:noHistory="true" ></activity>
        <activity android:name=".FunctionTab" android:theme="@style/hopeTheme" android:label="메인으로"></activity>
        <activity android:name=".BluetoothConActivity" />
        <activity android:name=".CheckTimeActivity" android:theme="@style/hopeTheme"
            android:label="홈으로"/>
        <activity android:name=".Manage" android:theme="@style/hopeTheme"/>
        <activity android:name=".StaticActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".Static2Activity" android:theme="@style/AppTheme"/>
        <activity android:name=".DevicelistActivity" android:theme="@style/AppTheme"/>

    </application>

</manifest>