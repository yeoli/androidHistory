<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demm.sibunmo_pratice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name = ".Join" android:label="Join"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".First" android:label="First"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".Blooth" android:label="Blooth"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".Jiji" android:label="Jiji"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".Mesure" android:label="Mesure"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".MesureResult" android:label="MesureResult"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".Result" android:label="Result"
            android:theme="@style/AppTheme"/>
        <activity android:name = ".Search" android:label="Search"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>