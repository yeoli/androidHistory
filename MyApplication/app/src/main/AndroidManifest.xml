<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yeol.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Bluetooth"
            android:label="아이디/비밀번호 찾기"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".DeviceActivity"
            android:label="회원가입"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".Four"
            android:label="처음으로"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".StartActivity"
            android:label="홈으로"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".ResultActivity"
            android:label="측정결과"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".StaticActivity"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".S2taticActivity"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".FiveTabActivity"
            android:theme="@style/zzTheme"></activity>
        <activity android:name=".UserActivity"
            android:theme="@style/MainTheme">
        </activity>
    </application>
</manifest>